# Copy this file to .env.local for local overrides (never commit .env.local)
# Leave empty in dev — Vite proxy handles /api → localhost:3001 automatically
VITE_API_URL=

# Set this to your deployed backend URL in production, e.g.:
# VITE_API_URL=https://your-backend.up.railway.app
